Build a vertical hello-world MVP before completing the full emulator. The immediate priority is a minimal emulator crate that consumes sw-rv32i-isa and sw-rv32i-asm, loads assembled bytes, executes only the instructions needed by the MVP hello-world source (addi, sb, ebreak), and proves the result by checking bytes written to memory. The broader CPU, memory, full RV32I execution, dump helpers, and complete assembler-backed examples remain queued for later incremental steps.