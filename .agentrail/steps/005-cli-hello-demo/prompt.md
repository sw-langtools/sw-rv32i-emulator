Add a CLI example for the MVP hello-world path. Create examples/asm/hello.s and examples/hello.rs. The example should read hello.s, assemble it with sw-rv32i-asm, print the source, print assembled bytes, decode/disassemble each instruction as the intermediate form, load the bytes into emulator memory, execute with a per-step trace showing pc and instruction text, then print final CPU state and a memory dump including the hello newline bytes at 0x100. Keep execution support limited to the existing MVP subset; do not implement unrelated RV32I instructions here. Run cargo fmt --all --check, cargo test, cargo clippy --all-targets --all-features -- -D warnings, sw-markdown-checker, and cargo run --example hello before committing and completing.