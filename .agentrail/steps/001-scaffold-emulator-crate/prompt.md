Scaffold the sw-rv32i-emulator Rust crate after sw-rv32i-isa has a usable register model, Instruction model, and decode API. Add Cargo.toml, src/lib.rs, module placeholders, README updates if appropriate, and initial smoke tests. Depend on sw-rv32i-isa and sw-isa-core using the repo's current local/path dependency pattern. Run cargo fmt and cargo test if available, then commit the scaffold before completing this step.