Implement RV32I loads, stores, FENCE, ECALL, and EBREAK behavior. Loads and stores should cover byte, halfword, and word widths, signed and unsigned load extension, bounds errors, and misaligned data access errors. Choose and document/test simple ECALL and EBREAK behavior suitable for demos, preferably typed trap errors or deterministic halt semantics. Add tests for width behavior, sign extension, endian layout, and error cases.