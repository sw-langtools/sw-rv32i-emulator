Implement execution for RV32I LUI, AUIPC, JAL, JALR, branches, register-immediate arithmetic/comparison/shifts, and register-register arithmetic/comparison/shifts. Use wrapping u32 semantics, signed comparisons where required, x0 write protection, branch/jump pc replacement, and JALR low-bit clearing. Add focused byte-loaded or direct-dispatch tests for edge cases including sign extension, negative branch offsets, x0 writes, and JAL/JALR link values.