Create the minimal sw-rv32i-emulator crate needed for the hello-world MVP. Scaffold Cargo.toml and src/lib.rs. Depend on sw-rv32i-isa and sw-rv32i-asm. Implement a small Memory type, CpuState with x0 enforcement and pc, and step/run support only for Instruction::OpImm Addi, Instruction::Store StoreWidth::Byte, and Instruction::Ebreak. Load assembled bytes into memory, fetch/decode through sw-rv32i-isa, and stop on EBREAK. Add an example or test that assembles the MVP hello-world source with sw-rv32i-asm, runs it, and asserts that memory at 0x100 contains hello newline. Do not implement unrelated RV32I instructions in this step. Run cargo fmt --all --check, cargo test, cargo clippy --all-targets --all-features -- -D warnings, and markdown checker before committing and completing.