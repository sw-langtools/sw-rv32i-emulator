Add byte-loaded emulator tests and examples that do not require the assembler yet. Use sw-rv32i-isa encode helpers or explicit bytes to load small programs into memory, run them, and inspect final registers and memory. Cover arithmetic, branch, jump, load/store, and halt/trap behavior. Examples should print source-equivalent comments, bytes, CPU state, and memory changes where useful.