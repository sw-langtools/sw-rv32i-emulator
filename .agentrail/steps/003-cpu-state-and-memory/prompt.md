Implement CpuState and Memory for RV32I execution. CpuState should track 32 u32 registers, pc, halted state, and instruction count, with x0 writes ignored through one write path. Memory should be little-endian byte-addressable Vec<u8> RAM with load/read/write helpers, bounds checks, and alignment errors where appropriate. Add focused tests for x0, pc updates, little-endian reads/writes, bounds errors, and misalignment behavior.