Start board/target support in sw-rv32i-target. Create or update an agentrail saga for board descriptions and blink demos. Add a TOML board schema for RISC-V boards with id, family, arch/profile, memory map, GPIO pins, aliases such as led, and MMIO device declarations. Implement loading/parsing of board TOML plus tests with at least ESP32-C3, ESP32-C5, and ESP32-C6 placeholder board files, and document that blink demos should bind to logical aliases so demos can be shared across boards that use the same GPIO pin.